# /daw — DAW command layer

Typed Go API over raw OSC addresses of the target DAW (REAPER first). Does not know about the agent at all — testable and drivable from the UI before the agent is wired in.
