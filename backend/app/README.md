# /app — Wails services

The public API surface the frontend calls into (auto-generated typed TS bindings). Keep signatures matching the UI↔Backend contract exactly — this is the seam the rest of the app depends on, don't change it without updating the contract doc first.
