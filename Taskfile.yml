version: '3'

vars:
  APP_NAME: "tonelab"
  BIN_DIR: "bin"
  PACKAGE_MANAGER: '{{.PACKAGE_MANAGER | default "npm"}}'
  VITE_PORT: '{{.WAILS_VITE_PORT | default 9245}}'
  # Target OS for build/package/run. Defaults to the host OS, and is overridden
  # by `wails3 build GOOS=...` (or the GOOS env var) for cross-compilation. The
  # tasks below dispatch to the matching platform Taskfile via this variable.
  GOOS: '{{.GOOS | default OS}}'

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml
  linux: ./build/linux/Taskfile.yml

tasks:
  build:
    summary: Builds the application
    cmds:
      - task: "{{.GOOS}}:build"

  package:
    summary: Packages a production build of the application
    cmds:
      - task: "{{.GOOS}}:package"

  run:
    summary: Runs the application
    cmds:
      - task: "{{.GOOS}}:run"

  test:
    summary: Runs the Go test suite (no DAW required)
    cmds:
      - go test ./...

  test:reaper:
    summary: Runs the round-trip tests against a real running REAPER
    cmds:
      # -p 1 because every round-trip test binds REAPER's one feedback port,
      # so the packages cannot run in parallel the way the normal suite does.
      - go test -tags reaper -count=1 -p 1 ./...

  test:ableton:
    summary: Runs the round-trip tests against a running Ableton Live with AbletonOSC
    cmds:
      - go test -tags ableton -count=1 ./backend/daw

  ci:
    summary: Everything the hosted CI used to run, on this machine. Vet and the race suite, the frontend typecheck, a macOS package, a Windows cross-build and the Linux build in Docker. Green here is the bar for merging into main.
    cmds:
      - gofmt -l . | tee /dev/stderr | test -z "$(cat)"
      - go vet ./...
      - go test -race -count=1 ./backend/... ./cmd/...
      - GOOS=windows CGO_ENABLED=0 go vet ./backend/... ./cmd/...
      - GOOS=linux CGO_ENABLED=0 go vet ./backend/... ./cmd/...
      - task: common:build:frontend
      - task: darwin:package
      - task: build:windows
      - task: build:linux
      - task: build:cli

  build:cli:
    summary: Builds the command line for this machine, Windows and Linux. The Linux one needs ALSA headers, so it is built in Docker like the app.
    cmds:
      - go build -trimpath -ldflags="-w -s" -o {{.BIN_DIR}}/tonelab-cli ./cmd/tonelab-cli
      - for: ["amd64", "arm64"]
        cmd: GOOS=windows GOARCH={{.ITEM}} CGO_ENABLED=0 go build -trimpath -ldflags="-w -s" -o {{.BIN_DIR}}/tonelab-cli-windows-{{.ITEM}}.exe ./cmd/tonelab-cli
      - for: ["amd64", "arm64"]
        cmd: >-
          docker run --rm --platform linux/{{.ITEM}} -v "$PWD":/src -w /src -e GOFLAGS=-buildvcs=false -e HOME=/tmp golang:1.25-bookworm bash -c
          'apt-get update -qq >/dev/null && apt-get install -y -qq libasound2-dev >/dev/null 2>&1
          && GOMODCACHE=/tmp/gomod GOCACHE=/tmp/gocache go build -trimpath -ldflags="-w -s"
          -o {{.BIN_DIR}}/tonelab-cli-linux-{{.ITEM}} ./cmd/tonelab-cli'

  build:windows:
    summary: Cross-compiles the Windows binaries from any host (no cgo on Windows)
    deps: [common:build:frontend]
    cmds:
      - GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -tags production -trimpath -ldflags="-w -s -H windowsgui" -o {{.BIN_DIR}}/{{.APP_NAME}}-windows-amd64.exe .
      - GOOS=windows GOARCH=arm64 CGO_ENABLED=0 go build -tags production -trimpath -ldflags="-w -s -H windowsgui" -o {{.BIN_DIR}}/{{.APP_NAME}}-windows-arm64.exe .

  build:linux:
    summary: Builds the Linux binaries in Docker, since they need GTK and WebKitGTK headers
    deps: [common:build:frontend]
    cmds:
      - for: ["amd64", "arm64"]
        cmd: >-
          docker run --rm --platform linux/{{.ITEM}} -v "$PWD":/src -w /src -e GOFLAGS=-buildvcs=false -e HOME=/tmp golang:1.25-bookworm bash -c
          'apt-get update -qq >/dev/null && apt-get install -y -qq libgtk-3-dev libwebkit2gtk-4.1-dev libasound2-dev >/dev/null 2>&1
          && GOMODCACHE=/tmp/gomod GOCACHE=/tmp/gocache go build -tags "production gtk3" -trimpath -ldflags="-w -s"
          -o {{.BIN_DIR}}/{{.APP_NAME}}-linux-{{.ITEM}} .'
