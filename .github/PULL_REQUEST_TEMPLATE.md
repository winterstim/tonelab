## What

## Why

Closes #

## How it was verified

- [ ] `task ci` passes
- [ ] Live suite run where the change touches a DAW or model (`task test:reaper`, `task test:ableton`, `-tags llm`)
- [ ] `wails3 build` still produces a working binary if the frontend or bindings changed
